// Long Version 
let str = '';
for(let i = 0; i <5;i++) {
    str += 'Hello'
}
console.log(str);

//Shorthand Version -> instead of a for loop use the repeat() sring method to repeat a string
const str = 'Hello '.repeat(5)
console.log(str)
