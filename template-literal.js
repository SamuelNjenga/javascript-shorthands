const name = 'JavaScript'
const day = 'Monday'

//Long Version
console.log('Hello '+ name+', it is '+day)

//Shorthand Version -> instead of using the + operator to concatenate strings we can use ES6 template literals
console.log(`Hello ${name}, it is ${day}`);